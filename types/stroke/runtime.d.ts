/**
 * Registers or updates host stroke hooks used by brush-tip loading code.
 *
 * @param {object} hooks
 */
export function setStrokeRuntime(hooks: object): void;
export function createTipSurface(width: any, height: any): never;
export function loadImageTip(src: any, imageToWhite: any): never;
